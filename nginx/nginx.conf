server {
    listen 80;
    
    location /api/ {
        proxy_set_header Host $http_host;
        proxy_pass http://backend:8000;
    }
    
    location /admin/ {
        proxy_set_header Host $http_host;
        proxy_pass http://backend:8000;
    }
    
    location /static/ {
        alias /staticfiles/;
        expires 30d;
    }
    
    location /media/ {
        alias /media/;
        expires 30d;
    }
    
    location / {
        alias /staticfiles/;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}